# E-Mail Benachrichtigungs-Konfiguration

# E-Mail-Benachrichtigungen aktivieren (true/false)
EMAIL_ENABLED=false

# SMTP Server Einstellungen
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentifizierung
SMTP_USER=deine-email@gmail.com
SMTP_PASS=dein-app-passwort

# Absender-Adresse (optional, verwendet SMTP_USER falls nicht gesetzt)
SMTP_FROM=Login Honeypot <deine-email@gmail.com>

# Empfänger für Benachrichtigungen
ALERT_EMAIL=deine-email@gmail.com

# Hinweise:
# - Für Gmail: Verwende ein App-spezifisches Passwort (nicht dein normales Passwort)
#   Erstelle ein App-Passwort unter: https://myaccount.google.com/apppasswords
# - Für andere Provider: Passe SMTP_HOST, SMTP_PORT und SMTP_SECURE entsprechend an
# - SMTP_SECURE=true für Port 465 (SSL), false für Port 587 (TLS) oder 25
